#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    int n;
    string s;
    cin >> n >> s;
    map<string, int> mp;

    for (int i = 0; i < n - 1; i++)
    {
        // string x =
        // cout << x << endl;
        // mp[x]++;
    }

    for (auto i : mp)
    {
        cout << i.first << " " << i.second << endl;
    }

    return 0;
}